const HEIGHT = 20;
const WIDTH = 10;
const SLOW_TICK = 25;
const FAST_TICK = 3;
const QUEUE_BAGS = 3;
const WAIT_BEFORE_SET = 500;  // how long to wait before setting the piece

export { HEIGHT, WIDTH, SLOW_TICK, FAST_TICK, QUEUE_BAGS, WAIT_BEFORE_SET };